import { H1 } from '../H1';
import { P } from '../P';

// eslint-disable-next-line
export const Div = ({ children }) => {
  return (
    <div className="App">
      <H1 />
      <P />
    </div>
  );
};
